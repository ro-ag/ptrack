<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- WKWebView runs out of process and the embedded terminal executes
         ordinary child processes, so the signed app needs no hardened-runtime
         exception or unsigned-executable-memory entitlement. -->
    <dict/>
</plist>
