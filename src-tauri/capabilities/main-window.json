{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the main application window to use Tauri core APIs only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
